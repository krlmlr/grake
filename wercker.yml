box: rocker/hadleyverse
build:
  steps:
    - script:
        name: install
        code: if ! make install; then fail "Install failed"; fi
    - script:
        name: test
        code: if ! make test; then fail "Test failed"; fi
    - script:
        name: wercker-deploy
        code: if ! make wercker-deploy; then fail "Moving output failed"; fi
